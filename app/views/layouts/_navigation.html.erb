<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold"><%= current_user.email if current_user %></strong>
                                <% if user_signed_in? %>
                                    <span class="text-muted text-xs block">Actions<b class="caret"></b></span>
                                <%end%>
                            </span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <% if user_signed_in? %>
                            <div>
                                <i class="fa fa-sign-out"></i><%= link_to 'Log out', destroy_user_session_path, :method => :delete %>
                            </div>
                        <%end%>
                    </ul>
                </div>
                <div class="logo-element">
                    SD
                </div>
            </li>
            <% if current_user && current_user.is_admin? %>
               
                
             
              
                <li class="<%= 'active' if current_page?('/orders') %>">
                    <%= link_to "/orders" do %>      
                        <i class="fa fa-file-code-o"></i> <span class="nav-label">Orders</span>
                    <%end%>
                </li>
                <li class="<%= 'active' if current_page?('/settings') %>">
                    <%= link_to "/settings" do %>      
                        <i class="fa fa-cogs"></i> <span class="nav-label">Settings</span>
                    <%end%>
                </li>
            <%end%>
        </ul>
    </div>
</nav>